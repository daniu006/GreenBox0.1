[build]
builder = "nixpacks"

[build.nixpacks]
buildCmd = "npm install && npx prisma generate && npm run build"

[deploy]
startCmd = "npx prisma migrate deploy && npm run start:prod"
healthcheckPath = "/health"
restartPolicyType = "on_failure"

[variables]
NIXPACKS_NODE_VERSION = "22"
